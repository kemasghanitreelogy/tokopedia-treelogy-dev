node_modules/
.env
.env.local
.vercel/
*.log
.env*.local

# local state store (sqlite) - real ledger data, never committed
state/

# exported spreadsheets - real buyer names, addresses and order values.
# A file already tracked stays tracked; this is about the ones we generate.
*.xlsx
